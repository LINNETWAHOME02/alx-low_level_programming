#include <stdio.h>
/**
 * main - This program is printing a statement using printf() instead of puts()
 * Return: 0 (Program executed successfully)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
