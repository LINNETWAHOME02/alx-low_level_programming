#include <stdio.h>
/**
 * main - This program prints a statement using put() instead of printf()
 * Return: 0 (Program executed successfully)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
